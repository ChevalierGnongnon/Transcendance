import {useState, useEffect} from 'react';
import HeaderOffline from './header-offline';
import HeaderOnline from './header-online';

function AppHeader(){
    const isAuthentificated = useState<true | false | null>(null);
}
export default AppHeader;